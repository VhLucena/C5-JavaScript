
// Working with arrays
let array = [];

// Adition value
array.push();

// Delete last value
array.pop();

// Delete first value
array.shift();

// Delete a specific set of values
firstIndex = 0
qtdElements = 2;
array.splice(firstIndex, qtdElements);

// Delete a specific set of values and including some values
firstIndex = 0
qtdElements = 3;
array.splice(firstIndex, qtdElements, listOfNewElements);

console.log(array);









console.log(array.length);